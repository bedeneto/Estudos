"""
                    Até 5 Kg           Acima de 5 Kg
File Duplo      R$ 4,90 por Kg          R$ 5,80 por Kg
Alcatra         R$ 5,90 por Kg          R$ 6,80 por Kg
Picanha         R$ 6,90 por Kg          R$ 7,80 por Kg

Se compra for feita no cartão Tabajara o cliente receberá ainda um desconto de 5% sobre o total da compra
Escreva um programa que peça o tipo e a quantidade de carne comprada pelo usuário e gere um cupom fiscal,
contendo as informações da compra: tipo e quantidade de carne, preço total, tipo de pagamento,
valor do desconto e valor a pagar.
- Tipo da carne
- Quantidade da carne
- Cupom fiscal:
    - tipo
    - quantidade
    - preço total
    - pagamento
    - desconto
    - valor pago"""

#tipo de carne
TC = input("Por favor, informe o tipo de carne comprada (ex.:FD, AL, PI): ").upper()
#Quantidade em kgs
QT = float(input("Agora indique a quantidade em Kgs da carne acima comprado pelo cliente: "))
#Pagamento
PG = input("A compra foi feita no cartão tabajara? (ex.:s ou n): ").lower()

#(FD*QT) * 0.95 --- desconto

if TC == 'FD':
    if QT < 5:
        FD = 4.90
        if PG == 's':
            TOTAL = round((FD * QT) * 0.95, 2)
            print(f"O cliente comprou Filé Duplo a R$4.90/Kg, comprou a quantidade de {QT}kg e pagou no cartão Tabajara recebendo 5% de desconto. O total da compra ficou o valor de R${TOTAL:.2f}")
        elif PG == 'n':
            TOTAL = round(FD * QT,2)
            print(f"O cliente comprou Filé Duplo a R$4.90/Kg, comprou a quantidade de {QT}kg, mas não pagou no cartão Tabajara. O total da compra ficou o valor de R${TOTAL:.2f}")
    elif QT > 5:
        FD = 5.80
        if PG == 's':
            TOTAL = round((FD * QT) * 0.95, 2)
            print(f"O cliente comprou Filé Duplo a R$5.80/Kg, comprou a quantidade de {QT}kg e pagou no cartão Tabajara recebendo 5% de desconto. O total da compra ficou o valor de R${TOTAL:.2f}")
        elif PG == 'n':
            TOTAL = round(FD * QT, 2)
            print(f"O cliente comprou Filé Duplo a R$5.80/Kg, comprou a quantidade de {QT}kg, mas não pagou no cartão Tabajara. O total da compra ficou o valor de R${TOTAL:.2f}")

if TC == 'AL':
    if QT < 5:
        FD = 5.90
        if PG == 's':
            TOTAL = round((FD * QT) * 0.95, 2)
            print(f"O cliente comprou Alcatra a R$5.90/Kg, comprou a quantidade de {QT}kg e pagou no cartão Tabajara recebendo 5% de desconto. O total da compra ficou o valor de R${(TOTAL):.2f}")
        elif PG == 'n':
            TOTAL = round(FD * QT, 2)
            print(f"O cliente comprou Alcatra a R$5.90/Kg, comprou a quantidade de {QT}kg, mas não pagou no cartão Tabajara. O total da compra ficou o valor de R${TOTAL:.2f}")
    elif QT > 5:
        FD = 6.80
        if PG == 's':
            TOTAL = round((FD * QT) * 0.95, 2)
            print(f"O cliente comprou Alcatra a R$6.80/Kg, comprou a quantidade de {QT}kg e pagou no cartão Tabajara recebendo 5% de desconto. O total da compra ficou o valor de R${TOTAL:.2f}")
        elif PG == 'n':
            TOTAL = round(FD * QT, 2)
            print(f"O cliente comprou Alcatra a R$6.80/Kg, comprou a quantidade de {QT}kg, mas não pagou no cartão Tabajara. O total da compra ficou o valor de R${TOTAL:.2f}")

if TC == 'PI':
    if QT < 5:
        FD = 6.90
        if PG == 's':
            TOTAL = round((FD * QT) * 0.95, 2)
            print(f"O cliente comprou Picanha a R$6.90/Kg, comprou a quantidade de {QT}kg e pagou no cartão Tabajara recebendo 5% de desconto. O total da compra ficou o valor de R${TOTAL:.2f}")
        elif PG == 'n':
            TOTAL = round(FD * QT, 2)
            print(f"O cliente comprou Picanha a R$6.90/Kg, comprou a quantidade de {QT}kg, mas não pagou no cartão Tabajara. O total da compra ficou o valor de R${TOTAL:.2f}")
    elif QT > 5:
        FD = 7.80
        if PG == 's':
            TOTAL = round((FD*QT)*0.95, 2)
            print(f"O cliente comprou Picanha a R$7.80/Kg, comprou a quantidade de {QT}kg e pagou no cartão Tabajara recebendo 5% de desconto. O total da compra ficou o valor de R${TOTAL:.2f}")
        elif PG == 'n':
            TOTAL = round(FD * QT, 2)
            print(f"O cliente comprou Picanha a R$7.80/Kg, comprou a quantidade de {QT}kg, mas não pagou no cartão Tabajara. O total da compra ficou o valor de R${TOTAL:.2f}")

